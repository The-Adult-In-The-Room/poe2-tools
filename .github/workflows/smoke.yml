name: Smoke Tests

on:
  push:
    branches: [main]

jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/playwright-test
        with:
          command: test:e2e:smoke
