name: PR Verification

on:
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: npm run verify

  acceptance-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/playwright-test
        with:
          command: test:e2e:acceptance
